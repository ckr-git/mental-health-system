# 系统架构总览

## 整体架构图

```mermaid
graph TB
    subgraph 客户端["客户端层"]
        Browser["浏览器<br/>Vue 3 + TypeScript + Element Plus"]
    end

    subgraph 前端应用["前端应用 (Port 3000/5173)"]
        direction TB
        Router["Vue Router<br/>路由守卫 + 角色鉴权"]
        Pinia["Pinia 状态管理<br/>用户状态 / Token 管理"]
        Axios["Axios HTTP 客户端<br/>请求拦截 / Token 注入"]
        STOMP["SockJS + STOMP<br/>WebSocket 客户端"]
        ECharts["ECharts 图表<br/>数据可视化"]
        Howler["Howler.js<br/>冥想音频播放"]
    end

    subgraph 网关层["API 网关层"]
        Proxy["Vite Dev Proxy / Nginx<br/>API 请求转发"]
    end

    subgraph 后端应用["后端应用 (Spring Boot 3.2 - Port 8080)"]
        direction TB
        subgraph 安全层["安全层"]
            SecurityFilter["Spring Security<br/>安全过滤链"]
            JWTFilter["JWT 认证过滤器<br/>Token 校验"]
            CORS["CORS 配置<br/>跨域处理"]
        end

        subgraph 控制层["控制层 (32 Controllers)"]
            AuthCtrl["AuthController<br/>认证接口"]
            PatientCtrl["Patient Controllers<br/>患者功能接口"]
            DoctorCtrl["Doctor Controllers<br/>医生功能接口"]
            AdminCtrl["Admin Controllers<br/>管理员接口"]
            WSCtrl["WebSocket Handler<br/>实时通信"]
        end

        subgraph 业务层["业务层 (Services)"]
            AuthSvc["AuthService<br/>认证服务"]
            MoodSvc["MoodDiaryService<br/>情绪日记"]
            AISvc["AIService<br/>AI 对话"]
            AppointSvc["AppointmentService<br/>预约管理"]
            CrisisSvc["CrisisAlertService<br/>危机预警"]
            ChatSvc["ChatService<br/>即时通讯"]
            AssessSvc["AssessmentService<br/>心理测评"]
        end

        subgraph 数据层["数据访问层"]
            MyBatisPlus["MyBatis-Plus ORM<br/>数据映射"]
            Flyway["Flyway<br/>数据库迁移"]
        end
    end

    subgraph 外部服务["外部服务"]
        AliAI["阿里云百炼 API<br/>qwen-plus 模型"]
    end

    subgraph 数据存储["数据存储层"]
        MySQL["MySQL 8.0<br/>Port 3306<br/>mental_health 库"]
        FileStore["文件存储<br/>./uploads/<br/>头像/附件"]
        Redis["Redis (可选)<br/>缓存层"]
    end

    Browser --> Router
    Router --> Pinia
    Router --> Axios
    Router --> STOMP
    Axios --> Proxy
    STOMP --> WSCtrl
    Proxy --> SecurityFilter
    SecurityFilter --> JWTFilter
    JWTFilter --> CORS
    CORS --> AuthCtrl
    CORS --> PatientCtrl
    CORS --> DoctorCtrl
    CORS --> AdminCtrl
    AuthCtrl --> AuthSvc
    PatientCtrl --> MoodSvc
    PatientCtrl --> AISvc
    PatientCtrl --> AppointSvc
    PatientCtrl --> AssessSvc
    DoctorCtrl --> CrisisSvc
    DoctorCtrl --> ChatSvc
    DoctorCtrl --> AppointSvc
    AdminCtrl --> AuthSvc
    AISvc --> AliAI
    MoodSvc --> MyBatisPlus
    AuthSvc --> MyBatisPlus
    AppointSvc --> MyBatisPlus
    CrisisSvc --> MyBatisPlus
    ChatSvc --> MyBatisPlus
    AssessSvc --> MyBatisPlus
    MyBatisPlus --> MySQL
    Flyway --> MySQL
    PatientCtrl --> FileStore

    style 客户端 fill:#E3F2FD,stroke:#1565C0
    style 前端应用 fill:#E8F5E9,stroke:#2E7D32
    style 网关层 fill:#FFF3E0,stroke:#E65100
    style 后端应用 fill:#FCE4EC,stroke:#C62828
    style 外部服务 fill:#F3E5F5,stroke:#6A1B9A
    style 数据存储 fill:#FFFDE7,stroke:#F57F17
```

## 技术栈速览

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端框架 | Vue 3 + TypeScript | 3.4 / 5.3 |
| UI 组件库 | Element Plus | 2.5 |
| 状态管理 | Pinia | 2.1.7 |
| 构建工具 | Vite | 5.0 |
| 后端框架 | Spring Boot | 3.2.0 |
| ORM | MyBatis-Plus | 3.5.5 |
| 安全 | Spring Security + JWT | - |
| 数据库 | MySQL | 8.0 |
| AI | 阿里云百炼 (qwen-plus) | - |
| 实时通信 | WebSocket (STOMP) | - |
