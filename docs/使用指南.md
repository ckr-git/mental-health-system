# 智能心理健康管理系统 - 使用指南

## 🚀 快速启动

### 首次运行

1. **初始化数据库**（仅第一次）
   ```
   双击运行: init-database.bat
   输入MySQL root密码
   ```

2. **启动所有服务**
   ```
   双击运行: start.bat
   ```
   
   启动顺序：Redis → MySQL → 后端 → 前端

3. **访问系统**
   - 前端：http://localhost:3000
   - 后端：http://localhost:8080

### 日常使用

```
启动：双击 start.bat
停止：双击 stop.bat
```

---

## 📋 核心脚本说明

| 脚本 | 用途 | 何时使用 |
|-----|------|---------|
| **start.bat** | 一键启动所有服务 | 每次开始工作 |
| **stop.bat** | 停止所有服务 | 结束工作时 |
| **init-database.bat** | 初始化数据库 | 仅首次运行 |
| **start-redis-windows.bat** | 单独启动Redis | 如需单独启动 |

---

## 🔧 独立启动（高级）

### 启动Redis
```
双击: start-redis-windows.bat
或在PowerShell: C:\Redis\redis-server.exe
```

### 启动后端
```powershell
cd "E:\ddd\智能心里健康管理系统"
mvn spring-boot:run
```

### 启动前端
```powershell
cd "E:\ddd\智能心里健康管理系统\frontend"
npm run dev
```

---

## 📝 测试账号

需要在数据库中手动添加：

```sql
USE mental_health;

-- 患者账号 (密码: 123456)
INSERT INTO user (username, password, nickname, phone, role, status) 
VALUES ('patient001', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z6pQE0eojCGnJQ0bXK4Ak9Ae', 
        '测试患者', '13800000001', 'PATIENT', 1);

-- 医生账号 (密码: 123456)
INSERT INTO user (username, password, nickname, phone, role, status) 
VALUES ('doctor001', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z6pQE0eojCGnJQ0bXK4Ak9Ae', 
        '张医生', '13900000001', 'DOCTOR', 1);

-- 管理员账号 (密码: 123456)
INSERT INTO user (username, password, nickname, phone, role, status) 
VALUES ('admin001', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z6pQE0eojCGnJQ0bXK4Ak9Ae', 
        '管理员', '13700000001', 'ADMIN', 1);
```

---

## ❓ 常见问题

### 端口被占用

```powershell
# 查看占用端口的进程
netstat -ano | findstr :8080

# 或运行 stop.bat 强制停止所有服务
```

### Redis连接失败

1. 检查Redis是否运行：
   ```powershell
   C:\Redis\redis-cli.exe ping
   ```
   应返回：PONG

2. 如未运行，启动Redis：
   ```
   双击: start-redis-windows.bat
   ```

### MySQL连接失败

1. 检查MySQL服务：
   ```powershell
   sc query MySQL80
   ```

2. 启动MySQL：
   ```powershell
   net start MySQL80
   ```

3. 检查配置文件密码：
   编辑 `src/main/resources/application.yml`

### 前端依赖问题

```powershell
cd frontend
rm -rf node_modules
npm install
```

---

## 🎯 开发提示

- **后端启动时间**：约30秒
- **前端热重载**：修改代码自动刷新
- **API文档**：http://localhost:8080/doc.html（如已配置）
- **数据库**：mental_health

---

## 📚 项目文档

- **完整文档**：README.md
- **快速上手**：QUICKSTART.md
- **本指南**：使用指南.md

---

**祝开发顺利！🎉**
